# SEO

The prototype includes a German title, description and theme color. The production build should add canonical URLs, `robots.txt`, sitemap, verified Organization / SportsActivityLocation JSON-LD, page-specific OpenGraph imagery and server-rendered metadata after the final domain is known.
