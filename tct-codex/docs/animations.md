# Motion

The design uses a compact motion vocabulary: entry reveal, headline rise, photo zoom, card lift, marquee and navigation-overlay transitions. All effects are CSS-native and decorative. `prefers-reduced-motion: reduce` turns motion and transitions off globally while leaving all content and navigation accessible.
