# Deployment

This prototype can be deployed as a static Vite build. Run `npm ci` and `npm run build`, then publish `dist/`. For the requested editorial CMS and secure form workflow, deploy a server-backed Next.js implementation instead; do not expose CMS credentials or form recipients in client-side code.
